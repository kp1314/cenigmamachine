#ifndef ENCODER
#define ENCODER

#include <iostream>
#include <fstream>
#include <vector>

const int Z_ASCII = 90;
const int A_ASCII = 65;
const int ALPHA_LENGTH = 26;
const int VECTOR_INITIALIZE = -1;

/*  class Encoder {
    public:
      virtual void encode(char&) =0;
    protected:
      std::vector<int> configArray{std::vector<int>(ALPHA_LENGTH          ,VECTOR_INITIALIZE)};  
  };*/   

#endif
