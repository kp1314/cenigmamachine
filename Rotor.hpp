#include <string>
#include <iostream>

class Rotor { 

  //Rotor takes in rotor configuration 
  public: 
    Rotor(instream);
    char encode(char);  
    void rotate(void);
  private:
    instream rotConfig;
    int numberOfRotations;
  
}
